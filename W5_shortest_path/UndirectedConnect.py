import networkx as nx
import matplotlib.pyplot as plt
import random


G = nx.read_adjlist('dijkstraData.txt')

# how to read adjlist in with length?

# how to generate as list of boolean for bookkeeping - mark

#should I just define a class, and put the boolean, the length, the length so far (A) all as attributes?
#what about linked list? just read in and add edges one by one?

def BFS(G, s)
    
    
